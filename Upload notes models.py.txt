class Notes(models.Model):
    user = models.ForeignKey(User,on_delete=models.CASCADE,null=True)
     uploadingdate = models.CharField(max_length = 10,null=True)
     branch = models.CharField(max_length=30, null=True)
     subject = models.CharField(max_length=30, null=True)
    notesfile = model.FileField(null=True)
    filetype = models.CharField(max_length=30, null=True)
    description = model.FileField(max_length=30,null=True)
     status = model.fileField(max_length=15, null=True)
    def __str__(self):
    return self.signup.user.username+" "+self.status